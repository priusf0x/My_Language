function sosal(var count, var amount, var meow)
{
    while (count > 0)
    {
        count = count - amount;
    }

    while (amount == 0)
    {
        amount = amount + 1;
        count = count - 1;
    }

    return 0;
}

function main()
{
    sosal (meow, bar, foo);

    return 0;
}
