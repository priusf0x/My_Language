#include "compiler.h"

#include "Assert.h"
#include <cstddef>

// =============================== MAIN_CYCLE =================================

compiler_return_e 
CompileBranch(ssize_t    node,
              compiler_t compiler)
{
    ASSERT(compiler != NULL);

    while (/*node != NO_LINK*/)
    {
        // compile 
    }

    return COMPILER_RETURN_SUCCESS;
}





