G = var_init ";" | func_def 

stmt = if             |
       while          |
       var_init  ";"  |
       return    ";"  |
       '{'{stmt} *'}' |
       expr      ";"  |

if = "if" '(' expr ')' stmt ( | else) 
while = "while" '(' expr ')' stmt ( | else)
else = "else" stmt
func_def = "function" id '(' {var_init} ')' stmt
var_init = "var" id ("=" expr) | ()
return = "return" expr 
assignment = "=" expr
expr = (term () | assignment) {"+" | "-" term}*
bool = unary "==" | ... | "!=" unary
unary = ("+" | "-" | ) primary 
primary = '(' expr ')'                      |
          (id | id '(' id {',' id}* ')'     |
          number                            |  
